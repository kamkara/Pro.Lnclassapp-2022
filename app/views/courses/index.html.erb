<% @title="Accueil" %>
<!-- Navbar -->
<%= render "courses/navbar" %>

<div class="w-full mt-20">
    <!-- Feed Course -->
    <div id="courses" class="mx-auto md:w-2/3 ">
        <% @courses.each do |course| %>
            <%= turbo_frame_tag "feed_course" do %>
                <%= link_to course_path(course) do %>
                    <div class="border m-2 bg-white">
                        <div class="text-xl p-2">
                            <h1 class="font-bold"><%= course.title %></h1>
                        </div>
                        <div class="border m-2 p-2">
                            <%= truncate(strip_tags(course.content.to_s.html_safe), length: 500) %>
                        </div>
                    </div>
                <% end %>
            <% end %>
        <% end %>
    </div>

</div>

